import React from "react";

const CallToAction = () => {
	return (
		<div className="cta">
			<button onClick={() => alert("Thank you for your interest!")}>
				Partner with Us
			</button>
		</div>
	);
};

export default CallToAction;
